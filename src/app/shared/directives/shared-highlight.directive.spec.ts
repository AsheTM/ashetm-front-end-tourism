import { SharedHighlightDirective } from './shared-highlight.directive';

describe('SharedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
