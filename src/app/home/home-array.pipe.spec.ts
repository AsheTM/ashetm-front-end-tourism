import { HomeArrayPipe } from './home-array.pipe';

describe('HomeArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new HomeArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
